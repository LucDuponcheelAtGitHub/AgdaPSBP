-- module Specifications.Stateful where

-- {-

-- below is the Lean code to deal with State

-- just use the normal Z, Y, X ... naming conventions
-- and use S for State

-- You probably do not need this outParam stuff



-- class WithState
--     (σ : outParam Type)
--     (program : Type → Type → Type) where
--   readState {α : Type} : program α σ
--   writeState : program σ Unit

-- -}